import java.text.DateFormat
import java.text.SimpleDateFormat

def getDateTime() {
    DateFormat df = new SimpleDateFormat("yyyyMMddHHmm");
    return df.format(new Date());
}

plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {

    // ----- BEGIN flavorDimensions (autogenerated by flutter_flavorizr) -----
    flavorDimensions += "flavors"

    productFlavors {
        dev {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_dev"
            resValue "string", "app_name", "[D] Pokedex Scaffold"
        }
        devh {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_dev"
            resValue "string", "app_name", "[D] Pokedex Scaffold"
        }
        qa {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_test"
            resValue "string", "app_name", "[T] Pokedex Scaffold"
        }
        qah {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_test"
            resValue "string", "app_name", "[T] Pokedex Scaffold"
        }
        prod {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold"
            resValue "string", "app_name", "Pokedex Scaffold"
        }
        prodh {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold"
            resValue "string", "app_name", "Pokedex Scaffold"
        }
    }

    // ----- END flavorDimensions (autogenerated by flutter_flavorizr) -----
    namespace = "com.example.pokedex_scaffold"

    // FLUTTER SDK VERSIONS ARE SPECIFIED ON flutter.gradle file located into the following path:
    // <flutter_sdk_path>/packages/flutter_tools/gradle/flutter.gradle
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.pokedex_scaffold"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    // Set informations with release keystore; to use them for a signed build, enable signing configs changing buildTypes release signingConfig option
    signingConfigs {
        create("release") {
            keyAlias = ""
            keyPassword = ""
            storeFile = file("/absolute/path/to/keystore")
            storePassword = ""
        }
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
            // signingConfig = signingConfigs.release
        }
    }

    applicationVariants.all { variant ->
        variant.outputs.all {
            outputFileName = "pokedex_scaffold_v${android.defaultConfig.versionName}_${android.defaultConfig.versionCode}-${variant.getFlavorName()}-${variant.buildType.name}-${getDateTime()}.apk"
        }
    }
}

flutter {
    source = "../.."
}

dependencies { 
    devImplementation "com.google.android.gms:play-services-base:17.3.0"
    qaImplementation "com.google.android.gms:play-services-base:17.3.0"
    prodImplementation "com.google.android.gms:play-services-base:17.3.0"
    devhImplementation "com.huawei.hms:hwid:6.12.0.300"
    qahImplementation "com.huawei.hms:hwid:6.12.0.300"
    prodhImplementation "com.huawei.hms:hwid:6.12.0.300"
 }