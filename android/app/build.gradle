plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {

    // ----- BEGIN flavorDimensions (autogenerated by flutter_flavorizr) -----
    flavorDimensions += "flavors"

    productFlavors {
        dev {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_dev"
            resValue "string", "app_name", "[D] Pokedex Scaffold"
        }
        devh {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_dev"
            resValue "string", "app_name", "[D] Pokedex Scaffold"
        }
        qa {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_test"
            resValue "string", "app_name", "[T] Pokedex Scaffold"
        }
        qah {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold_test"
            resValue "string", "app_name", "[T] Pokedex Scaffold"
        }
        prod {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold"
            resValue "string", "app_name", "Pokedex Scaffold"
        }
        prodh {
            dimension "flavors"
            applicationId "it.kitcarson88.pokedexscaffold"
            resValue "string", "app_name", "Pokedex Scaffold"
        }
    }

    // ----- END flavorDimensions (autogenerated by flutter_flavorizr) -----
    namespace = "com.example.pokedex_scaffold"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        // TODO: Specify your own unique Application ID (https://developer.android.com/studio/build/application-id.html).
        applicationId = "com.example.pokedex_scaffold"
        // You can update the following values to match your application needs.
        // For more information, see: https://flutter.dev/to/review-gradle-config.
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
